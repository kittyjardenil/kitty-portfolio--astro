---
//components
import Layout from '../layouts/Layout.astro';
import Nav from '../components/Nav.astro';
import MobileSwiper from '../components/MobileSwiper.astro';
import Projects from '../components/Projects.astro';
import DesktopNav from '../components/DesktopNav.astro';
import EmailCTA from '../components/EmailCTA.astro';

//styles
import '../styles/global.css';
import '../styles/typography.css';
---

<Layout title="Portfolio" bleedMarksColor='purple' backgroundColor='white'>
    <EmailCTA color="purple"/>
	<div class="smiley draggable">
		<img src="/icons/smiley.svg" alt="smiley icon">
	</div>
	<div class="photo-mug draggable">
		<img src="/icons/photo--mug.webp" alt="mug photo icon">
	</div>
	<div class="star draggable">
		<img src="/icons/star-polygon.svg" alt="star icon">
	</div>
	<main>
		<section>
			<div class="box">
				<div class="left-col">
					<div class="header">
						<Nav color='purple'/>
						<div class="copy">
							<h1>Designer, illustrator, and letterer based in Parañaque, Philippines.</h1>
							<span>Currently open for freelance work: book, editorial, merch, and other related print projects.</span>
						</div>
					</div>
					<DesktopNav color='purple' activePage='home'/>
				</div>
				<div class="right-col">
					<MobileSwiper/>
					<Projects/>
				</div>
			</div>
		</section>
	</main>
</Layout>

<style>
	.smiley {
		right: 24px;
		top: 80px;

		@media (width > 431px) {
			display: none;
		}

		@media (width > 1024px) {
			display: block;
			right: unset;
			left: 61px;
			top: 285px
		}

		@media (width > 1920px) {
			top: 341px
		}
	}
	.photo-mug{
		width: 90px;
		right: 47px;
		top: 226px;
		
		@media (width > 431px) {
			display: none;
		}

		@media (width > 1024px) {
			display: block;
			right: unset;
			left: 244px;
			top: 331px;
			width: 105px
		}

		@media (width > 1920px) {
			top: 376px
		}
	}
	.star {
		display: none;

		@media (width > 1024px) {
			display: block;
			left: 486px;
			top: 96px;
		}
	}
	.box {
		display: flex;
		flex-direction: column;
		justify-content: space-between;
		padding: 20px;
		gap: 20px;
		height: 100svh;
		box-sizing: border-box;

		@media (width > 431px) {
			padding: 40px 64px;
		}

		@media (width > 1024px) {
			padding: 64px;
			flex-direction: row;
			gap: 10%;
		}

		>.left-col {
			@media (width > 1024px) {
				display: flex;
				flex-direction: column;
				justify-content: space-between;
				width: 20%;
			}
			>.header {
				display: flex;
				flex-direction: column;
				gap: 24px;

				@media (width>431px) {
					gap: 24px;
				}

				@media (width>1024px) {
					gap: 24px;
				}

				>.copy {
					display: flex;
					flex-direction: column;
					gap: 8px;

					@media (width>431px) {
						gap: 16px;
					}

					>h1 {
						font-size: var(--h3);
						color: var(--purple);
						width: 254px;

						@media (width>431px) {
							font-size: var(--h2);
							width: 100%;
						}

						@media (width>1024px) {
							font-size: var(--h4);
						}

						@media (width >=1920px) {
							font-size: 2.0625rem;
							line-height: 110%;
							max-width: 352px;
						}
					}

					>span {
						font-size: var(--small);
						color: var(--gray);

						@media (width>431px) {
							font-size: var(--large);
						}

						@media (width>1024px) {
							font-size: var(--x-small);
						}

						@media (width >= 1920px) {
							font-size: 0.875rem;
							line-height: 140%;
							max-width: 300px;
						}

						>a {
							color: var(--gray);
							transition: var(--transition);

							&:hover {
								color: var(--purple);
							}
						}
					}
				}
			}
		}

		>.right-col {
			flex-grow: 1;

			@media (width > 1024px) {
				width: 70%;
				display: flex;
				flex-direction: column;
				justify-content: space-between;
			}
		}
	}
</style>
